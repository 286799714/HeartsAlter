# HeartsAlter 对局

HeartsAlter 是一个四人、筹码结算的红心大战变体：服务端拥有牌局真相，Godot 客户端负责呈现牌桌与玩家操作。

## 牌局语言

**牌**：由花色（梅花、方块、红桃、黑桃）和点数（2 到 A）组成的标准扑克牌；牌局永远不包含大小王。

**手牌**：某位玩家当前持有、尚未打出的牌。手牌只发送给拥有者。

**墩**：每位玩家各出一张牌组成的四张牌集合。首张牌决定领出花色，领出花色中点数最高的牌赢得这一墩。

**领出**：一墩中第一张打出的牌，以及由此确定的花色。玩家有领出花色时必须跟花色。

**红心破**：本局首次打出红桃后进入的状态；红心破前，玩家不能在领出时主动打出红桃，除非手中只剩红桃。

**点数**：每张红桃计 1 点，黑桃 Q 计 6 点，其余牌计 0 点。射月不属于本变体。

**底注**：每名玩家在牌局开始时投入的相同筹码数量。本 demo 默认使用 100 筹码；房间创建者可通过服务端房间选项调整为其他正整数。

**奖池**：四名玩家底注汇集后的筹码总额。

**请客玩家**：结算时点数等于全场最高点数的所有玩家；他们不从奖池取回筹码。若最高点数并列，所有并列者共同请客。

**加权分配**：请客玩家之外的玩家，按各自点数占这些玩家点数总和的比例分配奖池；整数余数按座位顺序稳定分配。

**座位顺序**：玩家进入房间的顺序。它同时用于轮流出牌、结算余数的稳定 tie-break，以及 UI 的固定位置。

**演示机器人**：由服务端创建的非网络座位。它拥有公开的玩家状态和服务端私有手牌，但不占用真实客户端连接；`bots: true` 房间会用 3 个演示机器人补齐四个座位。

## 客户端语言

**牌面资源**：`高清全套扑克牌/PNG` 中与牌 ID 同名的 PNG；客户端只复制并加载 52 张标准牌，不加载两个 Joker。

**牌桌**：Godot C# 场景中的中央墩区、四个座位区、奖池和结算面板。发牌、出牌、整理手牌均使用 `easeInOutSine` tween。连接服务器时，服务端是唯一的牌局裁判；无服务器时的离线模式仅用于展示牌面与动画，不代表可用于联网对局的状态。

## 大厅与准备房间

**大厅**：独立的 Colyseus `lobby` 房间，只同步可公开发现的房间摘要；创建或加入操作返回一次性的席位预约，随后客户端进入具体房间。

**准备房间**：具体牌局开始前的 `hearts` 房间阶段。四个席位按座位顺序展示，真实玩家可以切换准备状态；房主和机器人席位始终准备。

**房主**：创建房间时获得的第一个真实席位。房主固定为准备状态，只有房主可以添加机器人或在所有席位都准备后开启游戏；房主离开时由下一位真实玩家接任。

**机器人席位**：房主在空席位上添加的服务端合成玩家，公开显示为机器人并固定为准备状态；它不占用网络连接，进入牌局后沿用服务端自动出牌路径。

**牌桌就绪**：所有真实客户端完成 `Table.tscn` 初始化后发送的 `table_ready` 信号；服务端收齐后才洗牌发牌。

**发牌就绪**：客户端完成本地发牌 tween 后发送的 `deal_ready` 信号；服务端收齐后才开放首墩出牌。
